/** @type {import('next').NextConfig} */
const nextConfig = {
    // The 'runtime' option has been removed as it can conflict with the
    // @cloudflare/next-on-pages build process. The adapter handles this automatically.
};

export default nextConfig;
